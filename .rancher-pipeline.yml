stages:
- name: Publish image
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: ${CICD_GIT_REPO_NAME}:latest
      pushRemote: true
      registry: index.docker.io
branch:
  include:
  - master
notification: {}
